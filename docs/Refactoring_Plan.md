# План рефакторинга

Цель: обычное изменение предмета, персонажа или интерфейса должно требовать
понимания небольшого числа связанных мест. Сохраняем компоненты и ресурсы,
но делаем их удобными для автора контента и разработчика.

## Порядок работы

- Завершаем небольшую задачу, проверяем затронутое поведение и делаем коммит.
- В коммит включаем только изменения текущей задачи.
- Отмечаем выполненные пункты и результаты проверок в этом файле.
- Изменение игровых правил отделяем от переноса существующего кода.

## 1. Инвентарь и экипировка — первый этап

- [x] Перенести перемещение надетых предметов из `InventoryMenuComponent`
  в публичную операцию `EquipmentComponent`. Меню передаёт источник и цель;
  правила перемещения и восстановления экипировки принадлежат игровой логике.
  Проверки: пустой слот, обмен, неподходящий слот, разные наборы оружия,
  перетаскивание через меню.
- [x] Сделать изменение экипировки атомарным: сначала проверить итоговый набор,
  затем применить его и уведомить подписчиков. Неудачная операция не меняет
  оружие, боеприпасы, режим боя и не посылает сигналы промежуточного состояния.
- [x] Выделить карточку предмета и ячейку инвентаря в самостоятельные сцены.
  Перенести оформление в тему; меню оставить управление выбором и действиями.
- [x] Убрать из карточки неработающие параметры оружия. Перед каждым изменением
  проверить реальные места использования; сохранить данные ресурсов.
- [x] Возвращать из операций понятную причину отказа и показывать её в меню.
- [x] Определить владение паузой: закрытие инвентаря не должно отменять паузу,
  установленную другим экраном или сценарием.

## 2. Предсказуемые настройки предметов

- [x] Составить таблицу полей оружия: работает сейчас / запланировано / удалить.
  Особое внимание: скорость, дальность, moveset, stagger и scaling.
  Результат: [Item_Parameters.md](Item_Parameters.md).
- [x] Для нужных полей реализовать влияние на механику с проверкой результата.
  Неактивные настройки убрать из обычного Inspector или явно обозначить.
  Дальность ближнего оружия подключена. Остальные заготовки явно неактивны;
  граница рефакторинга и будущих боевых механик зафиксирована в Item_Parameters.md.
- [x] Завершить миграцию старых полей `ItemData` в профили после проверки
  ресурсов, тестов и требований к совместимости сохранений.

## 3. Создание и настройка врагов

- [x] Добавить проверку зависимостей и анимаций в редакторе до запуска игры.
- [x] Устранить ручное согласование длительности атаки между конфигурацией
  и анимацией: выбрать один источник значения и согласовать его потребителей.
- [x] Упростить создание самостоятельного врага из шаблона с копированием
  принадлежащих ему SpriteFrames и AnimationLibrary.
- [x] Сгруппировать основные настройки и проверить сценарий «создать врага
  с другим рисунком и атакой без изменения GDScript».

## 4. Состояние и управление персонажем

- [x] Явно определить роли способностей, проверки совместимости действий
  и состояния для отображения; согласовать код с Architecture_Rules.md.
- [x] Убрать дублирование состояния атаки и смерти в визуальном компоненте
  врага, сохранив реакцию на прерывания и смерть.
- [x] Отделить обработку команд игрока от хранения и правил экипировки.
- [x] Сделать понятные точки настройки движения, боя, характеристик и внешности
  в готовых сборках персонажей.

## 5. Тесты и навигация

- [x] Заменить проверки случайных отступов и оформления проверками поведения.
  Проверки обязательной структуры и доступности элементов сохранить.
- [x] Выделять повторяющиеся тестовые заготовки по мере изменения тестов.
- [x] Освободить оставшиеся объекты и ресурсы в тестовых заготовках:
  отдельный игровой запуск не должен сообщать об утечках при завершении.
- [x] Добавить краткий путеводитель: запуск песочницы, запуск тестов,
  где изменять предмет, врага, анимацию и интерфейс.
  Запуск тестов и песочницы уже описан в [Testing.md](Testing.md).
  Общая навигация: [Project_Guide.md](Project_Guide.md).

## Выполнено

- 2026-09-05: создан план по результатам разбора проекта. Реализация начата
  с границы между меню инвентаря и правилами экипировки.
- 2026-09-05: добавлен `tests/run_tests.gd` для проверки в игровом процессе
  без редакторских placeholder-экземпляров. Исходная проверка групп
  `equipment` и `inventory_menu`: 10/10. При завершении существующих тестов
  Godot сообщает об оставшихся объектах и двух ресурсах; это отдельная
  задача очистки тестовых заготовок.
- 2026-09-05: перемещение надетых предметов перенесено в
  `EquipmentComponent.move_equipped_item`. Меню только разбирает данные
  перетаскивания и вызывает операцию. Добавлены пять тестов перемещения,
  обмена, восстановления после отказа, разных наборов, пустого источника
  и выключенного компонента. Проверка `equipment inventory_menu`: 15/15.
  Существующая последовательность снятия и надевания сохранена; атомарность
  и отсутствие промежуточных сигналов остаются следующей задачей.
- 2026-09-05: надевание, снятие, обмен, восстановление и обновление экипировки
  после изменения инвентаря используют подготовку набора и единое применение.
  Подписчики видят итоговые руки, боеприпасы и режим боя. Отказ не прерывает
  атаку и сохраняет выбранные боеприпасы. При обмене двух наборов сначала
  освобождаются несовместимые боеприпасы, затем подбираются новые.
  Полный прогон: 169/169 в 31 группе; прежние сообщения об утечках остаются.
- 2026-09-05: добавлены `ui/ItemDetailsView.tscn`, `ui/ItemCell.tscn` и
  `ui/InventoryTheme.tres` внутри features/inventory. Формирование описания
  принадлежит карточке; меню управляет выбором и действиями. Проверки меню
  и быстрых слотов: 3/3. В запущенной песочнице проверены открытие инвентаря,
  выделение набора оружия и карточка при наведении.
- 2026-09-05: из карточки убраны неработающие speed/reach/scaling/stagger,
  moveset, тип урона и reload; из сводок — неиспользуемые stability и poise.
  Критический множитель показан для ближнего боя, где он применяется.
  Поля ресурсов сохранены, неактивные настройки помечены в Inspector.
  Добавлена таблица Item_Parameters.md. Проверки: 17/17.
- 2026-09-05: отказы в надевании и перемещении предметов сообщают причину
  через `operation_rejected`. Меню показывает нехватку характеристик,
  неподходящий слот, занятые руки или несовместимые боеприпасы. Старое сообщение
  скрывается после успешной операции, выбора предмета и повторного открытия.
  После снятия ограничения запуска проверка `equipment inventory_menu`: 18/18.
- 2026-09-05: инвентарь получает и освобождает собственный `PauseLease`.
  Несколько экранов могут независимо удерживать паузу; сохраняется и пауза,
  существовавшая до открытия меню. Закрытие, disable и выход из дерева
  освобождают только владение меню. Повторное открытие не создаёт лишнего
  владельца. После enable меню продолжает обрабатывать ввод во время паузы.
  Добавлены проверки нескольких владельцев, удаления владельца и полного
  жизненного цикла меню в настоящей сцене Player. Полный прогон: 174/174
  в 32 группах. Первый этап завершён; далее — настройки предметов.
- 2026-09-05: подключён reach_multiplier ближнего оружия. Зона удара меняется
  по горизонтали, сохраняет высоту и направление, возвращается к исходным
  размерам после завершения и disable. Повторные удары не накапливают масштаб;
  изменение предмета применяется со следующей атаки. Параметр возвращён
  в карточку и рабочую часть Inspector. Проверки дальности, экипировки
  и меню: 21/21. Это отдельное изменение механики, а не перенос кода.
- 2026-09-05: завершена миграция ItemData. Проверены все 51 ресурс предметов
  и сцены: старых плоских полей больше нет. Тестовые заготовки переведены
  на профили, удалены восемь дублирующих полей и запасные источники значений.
  Снимки восстановления хранят ссылки на ItemData в памяти; сохранений на диск
  в игровом коде нет. Полная проверка: 176/176 в 33 группах.
- 2026-09-05: обработка переключения оружия перенесена в необязательный
  EquipmentInputComponent. Он включён в Player; EquipmentComponent работает
  без InputComponent. Проверены переключение, отключение обработчика, экипировка
  и восстановление без ввода. Группы equipment, starter_equipment, quick_access,
  inventory_menu: 24/24. Этот независимый пункт этапа 4 выполнен раньше настроек
  врагов, поскольку завершает разделение правил экипировки и команд игрока.
- 2026-09-05: устранены утечки тестовых заготовок. В шести тестах вручную
  созданный DebugOverlayComponent не имел владельца и оставался в памяти.
  Панели зарегистрированы для очистки после теста через track(). Полный прогон:
  178/178 в 33 группах; завершение без ObjectDB leaks и оставшихся ресурсов.
- 2026-09-05: EnemyVisualComponent читает атаку и смерть из их владельцев,
  не хранит копии игровых флагов. Сигналы немедленно обновляют отображение,
  повторная инициализация не дублирует подключения. Уточнено различие между
  владельцами действий, проверками совместимости и сводкой ActorStateComponent.
  В обеих сценах врагов проверены атака, disable, новая атака и смерть:
  animation_pipeline, combat, actor_state — 54/54, без утечек.
- 2026-09-05: длительность атаки врагов берётся из одноразового клипа attack.
  AttackComponent получает AnimationPlayer по настраиваемому пути; пустой путь
  сохраняет конфигурационный режим игрока. Повторные числа времени удалены
  из шаблонов врагов. Проверена смена длины клипа для обычной и тяжёлой атаки,
  завершение и закрытие хитбокса: animation_pipeline, combat — 46/46.
  Актуальная инструкция: [Enemy_Timing.md](Enemy_Timing.md).
- 2026-09-05: в оба шаблона добавлен EnemyAuthoringChecks. Предупреждения
  редактора проверяют зависимости, конфигурации, кадры и клипы, источник
  времени, включённые события урона и порядок hitbox_on/hitbox_off. Проверка
  не инициализирует игровые компоненты. Тесты правильных и повреждённых сцен
  вместе с анимационными проверками: 8/8.
- 2026-09-06: добавлена кнопка Create independent copy в EnemyAuthoringChecks.
  Она сохраняет самостоятельную сцену, собственные SpriteFrames и
  AnimationLibrary, отдельные конфигурации компонентов. Исходная сцена
  не меняется; существующая папка защищена от перезаписи. Проверены оба
  шаблона, загрузка результата и независимость изменений ресурсов: 4/4.
  Инструкция: [Enemy_Copy.md](Enemy_Copy.md).
  Полный прогон после изменения: 184/184 в 34 группах, без утечек.
- 2026-09-06: настройки движения, фаз удара, полёта, характеристик и рисунка
  сгруппированы в Inspector. Сценарий копирования дополнен заменой кадра,
  длины клипа и урона, сохранением и запуском новой сцены без изменения скриптов.
  Добавлен Project_Guide.md с точками настройки и явной границей неактивных
  заготовок боевой механики.
- 2026-09-06: тесты меню и HUD больше не фиксируют отступы, число колонок,
  порядок ячеек, размер иконки и точные временные цвета. Проверяются состав
  слотов, клавиатурный фокус, читаемые подписи, диапазоны шкал и пропускание
  ввода фоном. Повторное создание экипируемого предмета вынесено в
  tests/fixtures/ItemFixtures.gd. Удалён устаревший UID ссылки на Player
  в песочнице; путь к сцене сохранён. Полный прогон: 184/184 в 34 группах,
  без ошибок и утечек. Все пункты текущего плана завершены.
