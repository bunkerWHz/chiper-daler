# Коллекция монстров

Готовые сцены находятся в `game/enemy/monsters`. Перетащите нужную `.tscn`
в уровень. Каждый монстр имеет собственные SpriteFrames, AnimationLibrary
и настройки компонентов; графика смотрит вправо, поворот влево делает flip_h.

## Новые монстры

| Папка ассетов | Имя | Тип | Сцена в game/enemy/monsters |
| --- | --- | --- | --- |
| BogStinger | BogStinger — болотный жалохвост | Летающий | `bog_stinger/BogStinger.tscn` |
| CaveImp | CaveImp — пещерный бес | Летающий | `cave_imp/CaveImp.tscn` |
| VenomHowler | VenomHowler — ядовитый ревун | Летающий | `venom_howler/VenomHowler.tscn` |
| EmberEye | EmberEye — угольный глаз | Летающий | `ember_eye/EmberEye.tscn` |
| HornedWretch | HornedWretch — рогатый злыдень | Наземный | `horned_wretch/HornedWretch.tscn` |
| AshRaptor | AshRaptor — пепельный ящер | Наземный | `ash_raptor/AshRaptor.tscn` |
| MossCrawler | MossCrawler — мшистый ползун | Наземный | `moss_crawler/MossCrawler.tscn` |
| RustCyclops | RustCyclops — ржавый циклоп | Наземный | `rust_cyclops/RustCyclops.tscn` |

StoneMaw и AmberWasp также остаются в коллекции. Папки ассетов всех монстров называются по имени: assets/Enemies/<Имя>/PNG Sequences.

Тип новых монстров определён по папке `Fly` внутри `PNG Sequences`.
В восьми новых папках ассетов оставлена только `PNG Sequences`; файлы AI,
EPS, TXT и альтернативные PNG больше не нужны для этих игровых сцен.

## Анимации и поведение

| Игровая анимация | Наземный | Летающий |
| --- | --- | --- |
| idle | Idle | Idle |
| move | Walking | Fly |
| airborne | Jump | Fall |
| attack | Attack | Attack |
| death | Dying | Dying |

Все клипы содержат 18 кадров и проигрываются со скоростью 18 кадров/с.
Атака и смерть не зациклены. AnimationPlayer содержит события открытия
и закрытия зоны удара, а также точки для звуков; звуковые файлы не назначены.

Наземные монстры используют GroundDummy, летающие — FlyDummy. Сохранены
базовые механики шаблонов: перемещение, обнаружение героя, ближняя атака,
здоровье, смерть и дроп. Названия не добавляют особых способностей:
например, VenomHowler пока не накладывает яд.

Коллизии подогнаны под тело без прозрачных полей и размаха крыльев.
Исходные спрайты имеют масштаб `1, 1`; общий размер задаёт корень монстра.
Размеры и боевые параметры — стартовая настройка для дальнейшего баланса.
По умолчанию выпадают янтарные осколки; таблица обычного дропа пустая:
[как изменить дроп](Loot_Setup.md).

## Посмотреть в игре

Для проверки по одному откройте `tests/EnemyPlatformSandbox.tscn` и нажмите F6.
После завершения анимации смерти текущего монстра появляется следующий.
В очередь включены все 10 монстров, после последнего она начинается заново.
Имя и номер текущего монстра отображаются в левом верхнем углу экрана.
Выпавший лут остаётся на уровне.

- Точка появления: переместите `EnemySpawn` (Marker2D) в дереве сцены.
  Ставьте его над платформой, оставляя место для тела монстра.
- Порядок и состав очереди: корень `EnemyPlatformSandbox` → **Enemy Scenes**.
  Переставляйте или добавляйте готовые сцены из коллекции.

Для одновременного размещения восьми новых монстров:

Откройте `tests/MonsterCollectionSandbox.tscn` и запустите текущую сцену (F6).
Герой начинает слева. Двигайтесь вправо по длинной платформе; подписи на ней
показывают начальные позиции монстров. Они могут перемещаться и преследовать
героя. В сцене доступны обычный бой, инвентарь и подбор мешков кнопкой R.

Для создания следующего монстра используйте пустые GroundDummy и FlyDummy:
[инструкция копирования](Enemy_Copy.md).
