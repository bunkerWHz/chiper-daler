---
title: Журнал
type: log
created: 2026-09-20
updated: 2026-09-23
tags: [meta]
---

# Журнал

Append-only хронология операций над wiki. Записи только добавляются в конец.
Каждая начинается с одинакового префикса, поэтому журнал разбирается обычными
утилитами:

```sh
grep "^## \[" log.md | tail -5
```

Формат записи: `## [ГГГГ-ММ-ДД] <операция> | <тема>`, где операция — `bootstrap`,
`ingest`, `query` или `lint`.

## [2026-09-20] bootstrap | docs/ стал Obsidian-vault и wiki-слоем

- Vault создан в самой папке `docs/`: файлы не переезжали, поэтому ссылки на
  `docs/*.md` из `game/`, `levels/` и `art/` README остались рабочими.
- Добавлена схема [[AGENTS]]: слои raw / wiki / schema, типы страниц,
  frontmatter, правила ссылок, операции ingest / query / lint.
- Создан навигационный слой: [[index]], [[overview]], [[concepts]], [[sources]]
  и этот журнал.
- Frontmatter добавлен во все 24 существующие страницы. Даты взяты из git:
  `created` — первый коммит файла, `updated` — последнее изменение содержимого.
- Конфигурация vault — `docs/.obsidian` в формате Obsidian 1.13.7: только
  встроенные плагины, граф раскрашен по полю `type`, workspace-состояние не
  коммитится.
- Добавлен локальный линтер `docs/tools/wiki_lint.mjs` — без зависимостей,
  только чтение.
- Границы работы: raw-источники не менялись, README вне vault не дублируются,
  существующие markdown-ссылки не переписывались в wikilinks.

## [2026-09-20] lint | Проверка после сборки vault

`node tools/wiki_lint.mjs`: 29 страниц, 0 ошибок, 0 предупреждений.

Исправлено по ходу проверки:

- экранированный `\|` в wikilinks внутри таблиц — линтер теперь его понимает;
- литеральные `[[wikilinks]]` в примерах схемы — линтер игнорирует inline-код;
- правило количества тегов смягчено до одного–четырёх на страницу.

Находки для владельца, **не переписаны** (решение по формулировкам остаётся за
автором страниц). Три страницы описывают прежнюю схему награды за убийство —
опыт в `ProgressionComponent` и предмет Experience Tonic, — тогда как код и
[[Amber_Economy|янтарные осколки]] говорят об обратном:

| Страница | Что устарело | Доказательство |
| --- | --- | --- |
| [[Game_Loop]] | Разделы «Enemy experience rewards» и «Loot drops» | `features/progression/ExperienceRewardComponent.gd` создаёт `AmberShardPickup`, а не начисляет опыт |
| [[Components]] | Карточка `ExperienceRewardComponent` и строка быстрого выбора | тот же скрипт, комментарий «Legacy name preserves authored enemy scenes and reward amounts» |
| [[Inventory_System]] | Две Experience Tonics в стартовом инвентаре и дроп тестового врага | `tests/test_starter_equipment.gd` проверяет отсутствие предмета `experience_tonic` |

Рекомендуемое действие по правилу схемы: пометить старые абзацы как
заменённые решением от 2026-09-15, не удаляя историю.

## [2026-09-20] ingest | Опыт только через янтарные осколки

Владелец подтвердил правило: опыт в игре добывается только через янтарные
осколки, томов опыта быть не должно. Устаревшие абзацы, найденные линтером,
приведены к текущему состоянию.

- [[Game_Loop]]: раздел «Enemy experience rewards» переписан в «Enemy rewards» —
  компонент с историческим именем роняет `AmberShardPickup` (25 по умолчанию),
  награда привязана к смерти, а не к добивающему удару. Раздел «Loot drops»
  больше не упоминает Experience Tonic: у тестового врага пустая таблица дропа,
  поэтому мешок не создаётся. В описании HUD добавлена шкала выносливости и
  сказано, что опыт отражает покупку уровня, а не награду за убийство.
- [[Components]]: карточка `ExperienceRewardComponent` переименована в «Осколки
  за убийство» и описывает валюту; карточка `ProgressionComponent` указывает,
  что уровень покупается в убежище; строка быстрого выбора — «Дать врагу
  награду и лут».
- [[Inventory_System]]: из стартового инвентаря убраны две Experience Tonics,
  описание дропа тестового врага приведено к пустой таблице.
- [[concepts]]: понятия «Янтарные осколки», «Опыт» и «Покупка уровня»
  сформулированы через правило 1 осколок = 1 опыт.
- [[overview]]: раздел про расхождения заменён записью об исправлении.

Основание — код: `features/progression/ExperienceRewardComponent.gd` создаёт
`AmberShardPickup`, `features/loot/AmberShardPickup.gd` кладёт валюту в
`InventoryComponent`, `features/rest/RestPoint.gd` продаёт уровень за осколки.

`ItemData.UseEffect.GRANT_EXPERIENCE` остаётся в коде, но ни один ресурс
предмета его не использует. Удаление значения из enum не выполнялось
намеренно: `APPLY_BUFF` идёт следом, и сдвиг сломал бы `use_effect = 4` у
`game/items/consumables/RagePotion.tres`.

## [2026-09-20] ingest | Вес и цена продажи при создании предмета

Правило владельца: новый предмет получает вес 1 и цену продажи, предметов без
веса быть не должно. Исключение — фласки: они не продаются и ничего не весят.

Причина правки: [[Item_Creation_Design]] с 2026-09-17 обещал стартовые значения
вес 1 и цена 1, но в `ItemData.gd` стояли `weight = 0.0` и `sell_price = 0`.
Из-за этого 11 из 14 шаблонов и два боевых предмета рождались без веса, а
фласки, наоборот, имели вес 0.25 у каждой.

- `features/inventory/ItemData.gd`: дефолты стали `weight = 1.0` и
  `sell_price = 1` с поясняющими комментариями. Шаблоны без собственных строк
  веса и цены наследуют 1, отдельная правка их ресурсов не нужна.
- Фласки приведены к правилу: `HealthPotion`, `ManaPotion`, `RagePotion` —
  вес 0 вместо 0.25; у маны и ярости цена продажи 8 и 12 заменена на 0, у
  здоровья строка цены добавлена явно.
- `GothicSwrods.tres` переименован в `GothicSwords.tres`, id `1000` заменён на
  `gothic_swords`, ссылка в `InventoryComponent.tscn` обновлена.
- `tests/test_item_authoring.gd`: новые проверки — новый предмет стартует с
  весом 1 и ценой 1, копия шаблона наследует их, у всех предметов каталога есть
  вес и цена, а у фласок ровно 0 и 0.
- Страницы: [[Item_Creation_Design]], [[Item_Parameters]] (новый раздел «Вес и
  цена продажи»), [[Inventory_System]], [[concepts]], [[index]].

Проверки: `tests/run_tests.gd` — 281 тест, 281 пройден, 0 падений (профиль
`user://` вынесен в `.godot/test-run-profile`, как описано в [[Testing]]);
`node docs/tools/wiki_lint.mjs` — 30 страниц без замечаний.

Осознанная граница объёма: жёсткой проверки в коде нет — фласка с ненулевым
весом остаётся возможной ошибкой автора, её ловит тест каталога, а не движок.
Карточка предмета по-прежнему показывает «Weight: N Sell: N» и для фласок,
то есть «Sell: 0» у них видно явно.

## [2026-09-20] ingest | README предметов приведён к новым дефолтам

`game/items/README.md` утверждал, что целевые стартовые значения живут только в
документации и шаблоны к ним не приведены. Для веса и цены продажи это перестало
быть верным после правки дефолтов `ItemData`.

- Вводная строка README теперь говорит, что вес и цена продажи заданы дефолтами
  1 и 1 и достаются шаблонам и новым предметам сразу, а не полностью
  приведёнными остаются только прочие целевые значения.
- Шаг «Создать предмет» просит проверить массу и цену под предмет и напоминает,
  что у фласок они остаются нулевыми.
- Схема [[AGENTS|wiki]] уточнена: README проекта — авторская документация, а не
  замороженная история. Read-only для raw-источников означает, что wiki не
  переписывает их по своей инициативе при компиляции; по прямой просьбе
  владельца такой файл исправляется как любой другой, с записью в журнал.

## [2026-09-20] ingest | Правило «сначала wiki» перенесено в корневой AGENTS.md

Разбор механики DSH (пакет `@deepseek-ai/dsh-agent-instructions`) показал, что в
базовую цепочку инструкций сессии попадают только файлы `AGENTS.md` в каталогах
от корня проекта до рабочего каталога сессии. Рабочий каталог — корень
репозитория, поэтому `docs/AGENTS.md` в базовую цепочку не входит: он
подгружается отдельно, при первом успешном `read`, `write` или `edit` в каталоге
`docs/`. Значит, агент, который сразу правит `features/`, видел указатель на
вики, но не её протокол.

- Раздел «Documentation» в корневом `AGENTS.md` переписан в императив: перед
  изменением или объяснением механики прочитать ведущую страницу, начав с
  `docs/index.md`, и сверять конкретные значения с кодом, а не с вики.
- Закреплено требование обновлять ведущую страницу, её `updated` и `index.md` в
  том же коммите, что и изменение поведения; вытесненное правило помечается, а
  не удаляется.
- Добавлена отсылка: полный протокол живёт в `docs/AGENTS.md` и применяется к
  работе под `docs/`.
- Проверено, что `node docs/tools/wiki_lint.mjs` работает из корня репозитория.

Чего это не закрывает: принудительного поиска по вики в DSH нет, обновление
контекста идёт по касанию файловыми инструментами, а не через shell, и память
между сессиями не переносится. Вики остаётся durable-памятью проекта, но
обращение к ней опирается на эту инструкцию.

## [2026-09-20] ingest | Версия Godot в Testing.md приведена к фактической

Проверка соединения с редактором через godot-ai показала: сессия
`chiper-daler@6aee7a6d90d7e0d9` активна, Godot 4.7.2-stable, плагин и сервер
4.1.0, протокол 2, запуск сервера через `uvx`, текущая сцена —
`res://tests/MovementSandbox.tscn`, воспроизведение остановлено.

При этом [[Testing]] требовала Godot 4.6.1, хотя `project.godot` объявляет
набор функций 4.7, а игровые тесты на 4.7.2 проходят полностью.

- Требование в разделе «Все игровые тесты» поднято до Godot 4.7, рядом
  зафиксировано, на чём проверено: 4.7.2-stable и плагин 4.1.0.
- Пример консольного файла заменён на `Godot_v4.7.2-stable_win64_console.exe`.

Проверка: `node docs/tools/wiki_lint.mjs` — 30 страниц без замечаний.

## [2026-09-20] ingest | Сплошная проверка предметов и план по арту

Владелец попросил пройтись по уже созданным предметам. Проверены все 58 ресурсов
в `game/items`: 45 предметов каталога и 13 шаблонов.

Итог: **нарушений установленных правил нет ни у одного предмета** — вес и цена,
`is_valid()`, уникальность `id`, согласованность категорий и профилей, стопки
проходят у всех. Отдельно проверено, что каждый предмет на что-то ссылается
(от одной ссылки у `gothic_swords` до двенадцати у `training_sword`), поэтому
удалять в каталоге нечего.

Найдены пробелы, а не ошибки: 38 предметов без иконки (арта для оружия и брони
в проекте нет вовсе — папки `assets/items/Weapons` и `assets/items/Armors`
пусты), 14 единиц без мирового арта (13 видов оружия и `Training Greatshield`
скрыты в руке), 9 файлов графики не подключены ни к чему.

Две мои первые проверки оказались неверными и исправлены до отчёта:

- «нет иконки» я сначала пометил ошибкой, но по документации стартовое значение
  иконки — общая заглушка, и `get_display_icon()` подставляет её сама;
- «нет текстуры в руках» я сначала требовал и от брони, тогда как
  [[Inventory_System]] прямо говорит: броня и украшения на герое не
  отображаются, «replacing body artwork is not implemented».

Создано:

- [[Item_Plan]] — страница типа `plan` с чеклистом задач T1–T6 и полной таблицей
  45 предметов: слот, вес, цена, состояние иконки и мирового арта.
- `tests/item_audit.gd` — аудитор, который собирает этот список повторно. Только
  чтение; различает `error` (нарушение правила), `pending` (пробел из плана) и
  `note` (осознанная схема, похожая на пропуск). Тестовый раннер его не
  подхватывает, потому что имя не начинается с `test_`.

Обсуждение удаления: владелец предложил удалить предметы, не проходящие
проверки. Показано, что под правило «иконка обязательна» попали бы 38 из 45
предметов — вся броня и 13 из 15 единиц оружия, — а вместе с ними каталог
инвентаря, наборы экипировки, магазин и около десяти наборов тестов. Решено
ничего не удалять и вести список дел.

Проверки: `node docs/tools/wiki_lint.mjs` и `tests/run_tests.gd`.

## [2026-09-20] ingest | Вес в инвентаре стал общим

Владелец сообщил о баге: строка веса в инвентаре не учитывала надетые предметы.

Причина: `InventoryMenuComponent._rebuild_inventory_summary()` считал вес сам —
по «неодетым» стопкам из `_get_unequipped_stacks()`, — тогда как
`InventoryComponent.get_total_weight()` суммирует все стопки, включая надетые.
Экипировка резервирует предметы, не изымая их из инвентаря, поэтому общий вес
уже был доступен и просто не использовался.

- Строка инвентаря теперь берёт `_inventory.get_total_weight()`: сумка и
  надетое считаются вместе. Панель экипировки по-прежнему показывает отдельно
  Load — вес надетого против предела нагрузки.
- Локальная переменная `visible_weight` удалена; количество занятых слотов
  по-прежнему считается по неодетым стопкам.
- Тест `inventory_menu`: новый случай — меч весом 4 надевается, в сумке остаётся
  камень 2,5 × 2, строка показывает 9, а сумка — 5.
- Документация: [[Inventory_System]] описывает семантику, [[Item_Parameters]]
  уточняет потребителя поля weight.

Общий вес полон по построению: `_get_loadout_failure()` отклоняет экипировку
предмета, которого нет в инвентаре, — «This item is no longer in your
inventory», — поэтому надетый предмет всегда лежит в стопках.

Проверки: `tests/run_tests.gd` — 282 теста, 0 падений; `node
docs/tools/wiki_lint.mjs` — 31 страница без замечаний.

## [2026-09-20] ingest | Отдых восстанавливает выносливость полностью

Владелец уточнил правило: отдых у RestPoint восстанавливает здоровье, ману и
фляконы, задаёт точку возрождения — и должен также полностью восстанавливать
стамину.

`RestComponent.start_rest()` лечил, снимал дебаффы, восстанавливал ману и
пополнял фляги, но выносливость не трогал: персонаж, пришедший к костру
выдохшимся, оставался таким же и после отдыха.

- `RestComponent` теперь ищет `StaminaComponent` тем же способом, что
  `MagicComponent` и `FlaskChargesComponent`, и заполняет запас вызовом
  `restore(get_max_stamina())` — по образцу `heal(get_max_health())` и
  `restore_mana(get_max_mana())`. Нового API не потребовалось.
- Новый тест в наборе `rest`: тратится половина запаса, отдых запускается,
  стамина обязана сравняться с максимумом.
- Документация: карточка `RestComponent` в [[Components]] переписана — она
  утверждала «сам по себе не восстанавливает ману/выносливость», что было
  неверно уже про ману; список требуемых компонентов дополнен. [[Game_Loop]]
  перечисляет весь набор восстановления, включая фляконы, которых там не было.
  В [[Actor_Stats]] отмечено, что отдых восстанавливает стамину полностью.

Проверки: `tests/run_tests.gd` — 283 теста, 0 падений; `node
docs/tools/wiki_lint.mjs` — 31 страница без замечаний.

## [2026-09-20] ingest | Восстановление у костра мгновенное — это правило

Владелец подтвердил: отдых даёт мгновенное полное восстановление, а не
постепенное за время отдыха. Правило закреплено в документации явно, чтобы
`Duration` не начали со временем трактовать как длительность восстановления.

- [[Components]]: в ограничениях карточки `RestComponent` сказано, что
  восстановление применяется мгновенно в момент старта, а `Duration` только
  удерживает персонажа в состоянии отдыха и не растягивает восстановление.
- [[Game_Loop]]: восстановление помечено как мгновенное при взаимодействии.
- [[Actor_Stats]]: стамина восстанавливается полностью и сразу.
- [[concepts]]: понятие «Убежище и RestPoint» уточнено.

Кода это не меняло: `start_rest()` применяет лечение, ману, выносливость и
фляконы одной пачкой до `rest_started`, а таймер `Duration` идёт отдельно.

Проверка: `node docs/tools/wiki_lint.mjs`.

## [2026-09-21] ingest | Потолок 100 уровней, кривая порогов и две строки обмена

Система уровней получила потолок и опубликованную таблицу. Кривая осталась
геометрической, но прежний `Requirement Growth` 1.25 при 100 уровнях давал бы
на 99-м уровне порядка 10¹¹ осколков, поэтому значение заменено на 1.05.

- [[Level_Progression]] — новая страница: формула, поля `ProgressionConfig`,
  таблица всех 100 уровней (порог и накопленная сумма), контрольные точки,
  таблица чувствительности к `Requirement Growth`, две строки обмена и правила
  поведения на потолке. Статус: кривая, потолок и обмен — **Работает**;
  очки характеристик и навыков за уровень — **План**.
- [[Amber_Economy]]: раздел о покупке уровня теперь описывает обе строки обмена
  и потолок.
- [[Components]]: карточка `ProgressionComponent` дополнена потолком уровня и
  ссылкой на страницу уровней.
- [[concepts]]: понятия «Уровень персонажа», «Порог уровня», «Обмен всех
  осколков» и «Очки за уровень» добавлены; «Опыт» и «Покупка уровня» перевели
  ведущую страницу на [[Level_Progression]].
- [[index]]: страница добавлена в раздел справочников.

Код: `ProgressionConfig` получил `max_level` и новый рост; `ProgressionComponent`
останавливается на потолке, обнуляет остаток опыта и обрезает загруженный
уровень; `RestPoint` получил `get_full_exchange_cost` / `buy_all_levels` и
возвращает нулевую цену на потолке; `ShelterMenu` показывает две строки;
полоса опыта и оверлей отладки показывают максимум уровня.

Проверки: `tests/run_tests.gd` — 290 тестов пройдено, три падения в
`dot_resistances` воспроизводятся и на чистом дереве (запись в `user://`
недоступна в этом окружении) и к этой задаче не относятся. Новые тесты:
таблица порогов, сумма до потолка, остановка на потолке, обрезка уровня при
загрузке, обе строки обмена и обе строки меню убежища.

## [2026-09-21] lint | Проверка после добавления страницы уровней

- `node docs/tools/wiki_lint.mjs` — проверка frontmatter, ссылок, словаря
  тегов, покрытия индексом и страниц-сирот.

## [2026-09-21] ingest | Стартовый угол лука и арбалета, память угла последнего выстрела

Общее прицеливание получило стартовый угол для каждого действия и память угла
на 2 секунды. Прежнее правило «каждое новое прицеливание начинается с 5°,
предыдущий угол не сохраняется» отменено.

- [[Aiming]] — новый раздел «Память угла последнего выстрела»: окно 2 секунды,
  одно общее значение для всех четырёх действий, обнуление при перемещении
  и по концу окна, отмена подготовки память не трогает. Обновлены разделы
  «Согласованное поведение», «Жизненный цикл», «Разделение ответственности»
  и список проверок; `updated` — 2026-09-21.
- [[Components]]: карточка общего прицеливания дополнена стартовым углом
  владельца и памятью угла.
- [[concepts]]: понятия «Память угла последнего выстрела» и «Стартовый угол
  оружия» добавлены.
- [[index]]: сводка страницы прицеливания обновлена.
- [[Testing]]: добавлена команда новой сценовой проверки памяти угла.

Код: `AimingConfig` получил `angle_memory_duration` (2.0) и
`angle_memory_move_tolerance` (0.5); `AimingComponent.begin_aim` принимает
стартовый угол владельца, `end_aim` — флаг выстрела, который только и
записывает память; `RangedWeaponConfig.aim_default_angle_degrees` (0.0) задаёт
горизонтальный старт лука и арбалета; RangedWeaponComponent, ThrowingComponent
и MagicComponent сообщают о состоявшемся выпуске.

Заодно исправлена ручная площадка `tests/AimingSandbox.tscn`: F1–F4 не
переключали режим, потому что `_select_mode` пытался надеть вторую копию лука,
арбалета и посоха, уже находящихся во втором наборе оружия. Теперь площадка
переключает набор, которым владеет нужное оружие, и явно включает слот действия.

Проверки: `tests/run_tests.gd -- aiming` — 23 из 23; полный прогон — 296
пройдено, три падения в `dot_resistances` воспроизводятся и на чистом дереве.
Новые тесты: повторное прицеливание внутри окна, истечение окна, обнуление
перемещением с допуском, отмена без выстрела, общее значение для всех
владельцев и отключение памяти нулевой длительностью.
`tests/aiming_memory_runtime_check.gd` — режимы площадки, выстрел из лука,
память при стоянии на полу, повтор угла, стирание ходьбой и полные 2 секунды
окна; `tests/darklight_runtime_check.gd` и `tests/derived_stats_runtime_check.gd`
— без ошибок.

## [2026-09-23] ingest | Кувырок dodge2 и починка скиннинга рига Darklight

Добавлен авторский клип `dodge2` (`game/player/darklight/animations/dodge2.tres`,
0,4 с) — кувырок через голову: таз проворачивается на полный оборот, а все пять
IK/look-at целей облетают его, поэтому тело перекатывается через голову, спину,
бёдра и стопы и возвращается в ту же стойку, из которой начинало. Клип не
привязан к состоянию: `Dodge` по-прежнему играет `dodge`, длительность 0,18 с
не тронута. Клип в библиотеке `AnimationPlayer` рига и в тесте `darklight_visual`.

Клип генерируется, а не набивается руками: `tests/dodge2_roll_author.gd`
считает орбиты целей и высоту таза. Высота берётся из кадра: скрипт рендерит
риг в offscreen SubViewport и на каждом ключе ставит таз так, чтобы самая низкая
освещённая точка тела касалась линии пола, измеренной по `idle`. Режимы
`-- sheet` (контрольный лист), `-- frames` (позы текстом), `-- sweep`
(сравнение вариантов группировки), `-- measure`, `-- probe`.

Измерение вскрыло два дефекта рига, которые не видны в клипах с коротким шагом
таза и разрушают кувырок:

* веса скиннинга четырёх полигонов конечностей суммировались до 2,0 на вершину,
  поэтому их арт уезжал вдвое дальше костей; теперь суммы равны 1
  (`tests/normalize_skin_weights.mjs`, режим `--check`);
* три из четырёх полигонов конечностей дополнительно двигались
  `RemoteTransform2D`-ведомыми (позиция), то есть трансформация применялась
  дважды; ведомые выключены, как у четвёртого полигона и у плаща.

Внешний силуэт клипов `idle`, `run`, `jump`, `fall`, `heavy_attack` до и после
правки совпадает (замерялся тем же рендером). Тело в кувырке не проваливается
под пол ни на одном кадре; плащ (отдельный проигрыватель, свисает с таза)
на фазе вставания уходит под пол до 316 единиц собственным клипом `wind` и до
240 — позой `jump`: при подключении кувырка к состоянию плащу нужна подтянутая
поза.

## [2026-09-23] ingest | Замах в dodge2: присед и выпад

Клип `dodge2` удлинён до 1,0 с и получил замах перед кувырком. Раскладка: присед
0,00–0,14 с, выпад 0,14–0,42 с, кувырок 0,42–0,86 с, подъём 0,86–1,00 с.
Присед задан новой позой `SQUAT` (ноги согнуты, таз опускается на ~93 единицы,
руки отведены назад), выпад — позой `DIVE`: тело вытягивается по диагонали
вперёд-вниз (силуэт 991×697 против 718×1000 в стойке), обе руки выносятся
вперёд — кисть впереди головы на 67 единиц, ноги уходят назад-вниз. Кувырок и
подъём остались прежними: полный оборот таза, руки на пол в конце выпада и
таз, поднимающийся, пока голова проходит над землёй.

Инструмент получил взвешенное смешивание четырёх поз (`STAND`, `SQUAT`, `DIVE`,
`TUCK`) по кривым веса и режим `-- joints`: он печатает позиции таза, головы,
кистей и стоп по фазам, поэтому выпад проверяется геометрией, а не пикселями.
Тест `darklight_visual` теперь проверяет длину 1,0 с, присед (таз ниже стойки
более чем на 60 единиц), наклон выпада (40–90°), вынесенные вперёд руки
(> 180 единиц перед тазом) и уходящие назад ноги (< −180) — это и есть замах
как данные.

Пол по-прежнему держится: максимальное погружение тела +4,4 единицы на
интерполяции между ключами, спрайты +10,1. Плащ не изменился: до 311 единиц
ниже пола на приземлении клипом `wind`, 236 — позой `jump`.

## [2026-09-23] ingest | Горизонтальные руки в выпаде dodge2

В окне 0,24–0,40 с руки в выпаде держатся параллельно земле, и только после
этого персонаж сворачивается. Поза руки больше не берётся из таблицы поз:
генератор считает смещение кисти от текущего наклона корпуса (мировое
направление +X, обратный поворот на угол крена), поэтому кисть и плечо остаются
на одной высоте, пока корпус продолжает заваливаться вперёд. Замер: расхождение
по высоте 0 единиц на 0,25 / 0,30 / 0,35 / 0,40, угол в локте 155° (цепочка
187+88 = 275 единиц, вылет 270 — как и просили, до конца рука не выпрямляется).
До окна рука согнута (95° и 50° на 0,14 и 0,20), после — складывается (100° и
57° на 0,45 и 0,50), то есть клубок начинается после выпада.

Тест `darklight_visual` дополнен: на 0,25 / 0,32 / 0,40 кисть не отклоняется от
плеча по высоте больше чем на 12 единиц и вынесена вперёд больше чем на 200,
а ноги на 0,40 ещё разведены — сгибание начинается позже. 32 из 32 в
`darklight_visual animation_pipeline equipment_swap`.

## [2026-09-23] ingest | Кости и решатели рига

Новая страница [[Rig_Bones]]: практическая инструкция по скелету Darklight.
Разделы — слои рига, устройство `Bone2D`, порядок и индексы костей, добавление
кости, крепление рисунка (`RemoteTransform2D` против скиннинга `Polygon2D`),
решатели IK, зеркальный разворот, команды проверки и таблица частых ошибок.

Факты сверены с raw sources, а не по памяти: прогон `Bone2D` по обоим ригам
показал, что при включённом авторасчёте `length` и `bone_angle` выводятся из
первой дочерней кости (`FrontArmTop` → смещение `(-64, 176)` даёт
`length = 187.2752`, `bone_angle = 1.9196` рад), у листовых костей авторасчёт
выключен, а длина задана вручную (руки и голова 50, стопы 16). Суммы весов всех
четырёх скиннированных кусков равны 1.0.

Отдельно зафиксированы две ловушки. Первая: свойство `bone_angle` сериализуется
в градусах, а `get_bone_angle()` возвращает радианы, поэтому `bone_angle =
1.5707964` в `Cloak.tscn` грузится как 1.57°, а не 90°; у плаща нет решателей и
это не проявляется, но записывать так нельзя. Вторая: у нативных решателей
`flip_bend_direction` обратен одноимённому экспорту SoupIK — руки стоят с
`true`, ноги с `false`.

Обновлены [[Animation_Workflow]] и [[Project_Guide]] ссылкой на новую страницу,
[[concepts]] — четырьмя понятиями (кость рига, цели анимации, решатель IK,
скиннинг `Polygon2D`), [[index]] — записью в разделе «Инструкции».
## [2026-09-23] ingest | Копия рига стала производной, SoupGroup удалён

`DarklightRig2.tscn` больше не содержит нод SoupIK и не ссылается на
`addons/soupik`: решателям адресуются только цели `Anim Targets`, и на сами
узлы `SoupGroup` не ссылался ни один трек.

При проверке выяснилось, что копия уже отстала от исходного рига: пока она
существовала, в `DarklightRig.tscn` поменяли местами клипы `dodge` и `dodge2`, и
копия молча играла не тот клип. Поэтому сцена теперь генерируется скриптом
`tests/regenerate_darklight_rig2.mjs` из канонического рига, с режимом `--check`
против расхождений; страница [[Rig_Bones]] обновлена — раздел про две
реализации, инструкция «добавить решатель» и таблица проверок.

Новая проверка `tests/darklight_rig2_pose_check.gd` прогоняет все клипы в оба
рига и сравнивает все 15 костей плюс зеркальный разворот: 111 поз, максимум
0.005 единицы. Именно она поймала устаревшую карту клипов — сравнение одних
только целей его не видит, потому что локоть уходит на другую ветвь, а кисть
остаётся в цели.

## [2026-09-23] ingest | Уклонение разделено на dodge_air и dodge_roll

Клипы рига переименованы по назначению: `dodge` → `dodge_air`, `dodge2` →
`dodge_roll`; ресурс кувырка переехал в `animations/dodge_roll.tres`, генератор —
в `tests/dodge_roll_author.gd`. Обе карты клипов в `DarklightRig.tscn` и
`DarklightRig2.tscn` обновлены, копия пересобрана
`node tests/regenerate_darklight_rig2.mjs` (`--check` чист, 111 поз проверки
`darklight_rig2_pose_check.gd` совпадают, максимум 0.0054).

Вид уклонения теперь выбирает `DodgeComponent` в момент старта по
`is_on_floor()` и отдаёт его через `is_air_dodge()`: воздушное играет
`dodge_air`, наземное — кувырок `dodge_roll`. Визуальный компонент больше не
угадывает пол сам, поэтому клип и длительность не могут разойтись.

`DodgeConfig` получил `roll_duration` и `get_duration(is_air_dodge)`:
`duration` осталась длительностью воздушного уклонения (0.5 у игрока),
`roll_duration` = 0.45 у игрока, то есть кувырок играет авторский клип на ~2.2x,
а не на 5.5x, как при общей длительности. `darklight_visual` проверяет выбор
клипа и ретайминг по каждому режиму, `dodge` — что наземное уклонение переживает
воздушную длительность. Страница [[Components]] описывает оба поля,
[[Animation_Workflow]] и [[Testing]] — новый выбор клипа и переименованный
генератор.

Открытым остаётся плащ: у кувырка он всё ещё играет `wind` и на приземлении
уходит до 316 нативных единиц ниже пола, нужна подобранная поза.